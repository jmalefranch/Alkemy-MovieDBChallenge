# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AlkemyChallenge-MovieDB' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Pods for AlkemyChallenge-MovieDB
	pod "Alamofire"
	pod 'Kingfisher', '~> 7.0'
  
  target 'AlkemyChallenge-MovieDBTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AlkemyChallenge-MovieDBUITests' do
    # Pods for testing
  end

end


